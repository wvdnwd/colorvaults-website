{
  "name": "colorvaults",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "generate:data": "node scripts/generate-real-data.js",
    "generate:search": "node scripts/generate-search-index.js",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "generate:all": "npm run generate:data && npm run generate:search && npm run generate:sitemap",
    "prebuild": "npm run generate:search && npm run generate:sitemap"
  },
  "dependencies": {
    "framer-motion": "^13.0.0",
    "next": "16.2.11",
    "next-themes": "^0.4.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.11",
    "typescript": "^5"
  }
}
